trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
  - checkout: https://github.com/akash66sheoran/ansible-demo-repo.git

  - script: 'ls $(Agent.BuildDirectory)'

  - script: 'ls $(Build.SourcesDirectory)'